import { usePageTitle } from "../hooks";

const Achievements = () => {
    usePageTitle("Achievements");
    return (
        <div>
            <h1>Achievements</h1>
        </div>
    );
};

export default Achievements;